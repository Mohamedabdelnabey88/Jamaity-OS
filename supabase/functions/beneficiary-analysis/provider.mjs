import {reviewSchema,validModelReview} from './household-review.mjs';
export const MODEL='nvidia/nemotron-3-super-120b-a12b';
const instructions='أنت مساعد للباحث الاجتماعي. اكتب بالعربية ملخصًا محايدًا للإفادة وأسئلة استيضاح فقط. المدخل مؤشرات مالية وأعداد مصرح بها وغير متحقق منها. لا تصدر حكم استحقاق أو قبول أو رفض أو درجة أو ترتيب أولوية. لا تستنتج الاحتيال أو صفات شخصية. لا تدع قراءة مستندات. لا تخترع بيانات أو حدود فقر أو نسب ثقة. اربط كل سؤال بمفاتيح مصادره. نقص البيانات يستدعي الاستيضاح ولا يدل على قلة الاحتياج. اذكر حدود المعلومات. أقصى 6 أسئلة و4 حدود. لا تقترح مبلغ دعم.';
export async function nvidiaReview(key,deterministic,fetcher=fetch){
 const r=await fetcher('https://integrate.api.nvidia.com/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},signal:AbortSignal.timeout(30000),body:JSON.stringify({model:MODEL,stream:false,chat_template_kwargs:{enable_thinking:false},temperature:0.1,max_tokens:1500,response_format:{type:'json_object'},messages:[{role:'system',content:instructions+' أعد JSON فقط مطابقًا لهذا المخطط: '+JSON.stringify(reviewSchema)},{role:'user',content:JSON.stringify({reported:deterministic.numbers,calculated:deterministic.facts,missing:deterministic.missing})}]})});
 if(!r.ok)throw new Error('provider_failed');const data=await r.json();const choice=data.choices?.[0];if(choice?.finish_reason!=='stop')throw new Error('incomplete');const review=JSON.parse(choice.message.content);if(!validModelReview(review))throw new Error('invalid_output');return {review,model:MODEL,provider:'NVIDIA'};
}
