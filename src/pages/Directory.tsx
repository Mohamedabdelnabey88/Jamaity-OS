import {useEffect,useMemo,useState}from'react';import{Building2,MapPin,Search,ShieldCheck,ArrowLeft}from'lucide-react';import{useNavigate}from'react-router-dom';import{supabase}from'../supabase';
export default function Directory(){const[rows,setRows]=useState<any[]>([]),[q,setQ]=useState(''),[loading,setLoading]=useState(true);const n=useNavigate();useEffect(()=>{supabase.from('charities').select('id,name_ar,name_en,city,region,description_ar,logo_url,status').eq('status','approved').order('name_ar').then(({data})=>{setRows(data||[]);setLoading(false)})},[]);const shown=useMemo(()=>rows.filter(x=>`${x.name_ar||''} ${x.name_en||''} ${x.city||''} ${x.region||''}`.toLowerCase().includes(q.toLowerCase())),[rows,q]);return <main className="directory-page"><section className="directory-hero"><div><span className="eyebrow"><ShieldCheck size={15}/> دليل جمعيات جمعيتي</span><h1>اكتشف الجمعيات<br/><em>المعتمدة على المنصة.</em></h1><p>تصفح الجمعيات التي تمت الموافقة عليها داخل منصة جمعيتي، وتعرّف على مبادراتها وآخر أخبارها.</p></div><div className="directory-stat"><Building2 size={28}/><strong>{rows.length}</strong><span>جمعية متاحة</span></div></section><div className="directory-toolbar"><div className="search-box"><Search size={18}/><input value={q}onChange={e=>setQ(e.target.value)}placeholder="ابحث باسم الجمعية أو المدينة أو المنطقة..."/></div></div>{loading?<div className="empty">جاري تحميل دليل الجمعيات...</div>:!shown.length?<div className="empty"><Building2 size={40}/>لا توجد جمعية مطابقة.</div>:<section className="directory-grid">{shown.map(x=><article className="charity-card"key={x.id}onClick={()=>n(`/charity/${x.id}`)}>{x.logo_url?<img src={x.logo_url}alt=""/>:<div className="charity-logo"><Building2/></div>}<div className="charity-card-body"><div className="verified"><ShieldCheck size={14}/> معتمدة على المنصة</div><h2>{x.name_ar}</h2><span className="location"><MapPin size={14}/>{x.city||'السعودية'}{x.region&&` · ${x.region}`}</span><p>{x.description_ar||'صفحة الجمعية الرسمية على منصة جمعيتي.'}</p><button className="text-action">زيارة الصفحة <ArrowLeft size={16}/></button></div></article>)}</section>}</main>}
