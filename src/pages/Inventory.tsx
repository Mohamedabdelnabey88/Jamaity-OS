import {useEffect,useMemo,useState}from'react';import{Boxes,PackagePlus,RefreshCw,Search,Warehouse}from'lucide-react';import{supabase}from'../supabase';import{getAccessState}from'../lib/rbac';
export default function Inventory(){const[items,setItems]=useState<any[]>([]),[moves,setMoves]=useState<any[]>([]),[q,setQ]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState('');async function load(){setLoading(true);setError('');const a=await getAccessState();if(!a.charityId){setItems([]);setMoves([]);setLoading(false);return}const[i,m]=await Promise.all([supabase.from('inventory_items').select('id,sku,name_ar,name_en,unit,is_active').eq('charity_id',a.charityId).order('name_ar').limit(500),supabase.from('stock_movements').select('id,warehouse_id,item_id,movement_type,quantity,reference_type,created_at,warehouses(name_ar),inventory_items(name_ar,sku,unit)').eq('charity_id',a.charityId).order('created_at',{ascending:false}).limit(500)]);if(i.error||m.error)setError(i.error?.message||m.error?.message||'تعذر تحميل المخزون');setItems(i.data||[]);setMoves(m.data||[]);setLoading(false)}useEffect(()=>{load()},[]);const stock=useMemo(()=>{const s:Record<string,number>={};for(const m of moves){s[m.item_id]=(s[m.item_id]||0)+(m.movement_type==='receipt'?Number(m.quantity):m.movement_type==='issue'?-Number(m.quantity):0)}return s},[moves]);const shown=items.filter(x=>`${x.name_ar} ${x.sku}`.toLowerCase().includes(q.toLowerCase()));return <main className="module-page"><div className="module-head"><div><span className="eyebrow"><Boxes size={15}/> الموارد والمخزون</span><h1>إدارة المخزون</h1><p>رصيد الأصناف وحركات الاستلام والصرف المرتبطة بالعمليات الخيرية.</p></div><button className="secondary"onClick={load}><RefreshCw size={18}/> تحديث</button></div><div className="module-toolbar"><div className="search-box"><Search size={18}/><input value={q}onChange={e=>setQ(e.target.value)}placeholder="ابحث باسم الصنف أو SKU..."/></div></div>{error&&<div className="error">{error}</div>}<section className="data-card"><div className="data-card-head"><strong>{shown.length.toLocaleString('ar-SA')} صنف</strong><span><Warehouse size={14}/> الرصيد محسوب من حركات المخزون</span></div>{loading?<div className="empty">جاري تحميل المخزون...</div>:!shown.length?<div className="empty"><PackagePlus size={34}/>لا توجد أصناف مخزنية بعد.</div>:<div className="support-list">{shown.map(x=><article className="support-row"key={x.id}><div className="support-icon"><Boxes/></div><div className="support-main"><b>{x.name_ar}</b><span>{x.sku||'بدون SKU'} · {x.unit||'وحدة'}</span><small>{x.is_active?'نشط':'موقوف'}</small></div><div className="support-value"><strong>{(stock[x.id]||0).toLocaleString('ar-SA')}</strong> {x.unit||'وحدة'}</div><div className="workflow approved">رصيد متاح</div></article>)}</div>}</section></main>}
