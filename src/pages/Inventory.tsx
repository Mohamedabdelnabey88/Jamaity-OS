import{useEffect,useMemo,useState}from'react';import{Boxes,PackagePlus,Plus,RefreshCw,Search,Warehouse}from'lucide-react';import{supabase}from'../supabase';import{getAccessState}from'../lib/rbac';
export default function Inventory(){const[items,setItems]=useState<any[]>([]),[moves,setMoves]=useState<any[]>([]),[warehouses,setWarehouses]=useState<any[]>([]),[q,setQ]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState(''),[open,setOpen]=useState<'warehouse'|'item'|null>(null),[busy,setBusy]=useState(false),[form,setForm]=useState({nameAr:'',nameEn:'',sku:'',unit:'وحدة'});
 async function load(){setLoading(true);setError('');const a=await getAccessState();if(!a.charityId){setItems([]);setMoves([]);setWarehouses([]);setLoading(false);return}const[i,m,w]=await Promise.all([supabase.from('inventory_items').select('id,sku,name_ar,name_en,unit,is_active').eq('charity_id',a.charityId).order('name_ar').limit(500),supabase.from('stock_movements').select('id,warehouse_id,item_id,movement_type,quantity,reference_type,created_at,warehouses(name_ar),inventory_items(name_ar,sku,unit)').eq('charity_id',a.charityId).order('created_at',{ascending:false}).limit(500),supabase.from('warehouses').select('id,name_ar,name_en,is_active').eq('charity_id',a.charityId).order('name_ar')]);if(i.error||m.error||w.error)setError(i.error?.message||m.error?.message||w.error?.message||'تعذر تحميل المخزون');setItems(i.data||[]);setMoves(m.data||[]);setWarehouses(w.data||[]);setLoading(false)}useEffect(()=>{void load()},[]);
 const stock=useMemo(()=>{const s:Record<string,number>={};for(const m of moves)s[m.item_id]=(s[m.item_id]||0)+(m.movement_type==='receipt'?Number(m.quantity):m.movement_type==='issue'?-Number(m.quantity):0);return s},[moves]);const shown=items.filter(x=>`${x.name_ar} ${x.sku}`.toLowerCase().includes(q.toLowerCase()));
 async function create(e:any){e.preventDefault();setBusy(true);setError('');try{const fn=open==='warehouse'?'create_warehouse':'create_inventory_item';const args=open==='warehouse'?{p_name_ar:form.nameAr,p_name_en:form.nameEn||null}:{p_sku:form.sku||null,p_name_ar:form.nameAr,p_name_en:form.nameEn||null,p_unit:form.unit||'وحدة'};const{error:e}=await supabase.rpc(fn,args);if(e)throw e;setOpen(null);setForm({nameAr:'',nameEn:'',sku:'',unit:'وحدة'});await load()}catch(e:any){setError(e.message||'تعذر الحفظ')}finally{setBusy(false)}}
 return <main className="module-page"><div className="module-head"><div><span className="eyebrow"><Boxes size={15}/> الموارد والمخزون</span><h1>إدارة المخزون</h1><p>الأصناف والمستودعات وحركات الاستلام والصرف المرتبطة بالعمليات الخيرية.</p></div><div style={{display:'flex',gap:8}}><button className="secondary"onClick={()=>setOpen('warehouse')}><Warehouse size={18}/> مستودع جديد</button><button className="primary"onClick={()=>setOpen('item')}><Plus size={18}/> صنف جديد</button><button className="secondary"onClick={()=>void load()}><RefreshCw size={18}/> تحديث</button></div></div><div className="module-toolbar"><div className="search-box"><Search size={18}/><input value={q}onChange={e=>setQ(e.target.value)}placeholder="ابحث باسم الصنف أو SKU..."/></div></div>{error&&<div className="error">{error}</div>}<section className="data-card"><div className="data-card-head"><strong>{shown.length.toLocaleString('ar-SA')} صنف</strong><span><Warehouse size={14}/> {warehouses.length.toLocaleString('ar-SA')} مستودع · الرصيد محسوب من الحركات</span></div>{loading?<div className="empty">جاري تحميل المخزون...</div>:!shown.length?<div className="empty"><PackagePlus size={34}/>لا توجد أصناف مخزنية بعد.</div>:<div className="support-list">{shown.map(x=><article className="support-row"key={x.id}><div className="support-icon"><Boxes/></div><div className="support-main"><b>{x.name_ar}</b><span>{x.sku||'بدون SKU'} · {x.unit||'وحدة'}</span><small>{x.is_active?'نشط':'موقوف'}</small></div><div className="support-value"><strong>{(stock[x.id]||0).toLocaleString('ar-SA')}</strong> {x.unit||'وحدة'}</div><div className="workflow approved">رصيد متاح</div></article>)}</div>}</section>{open&&<div className="modal-backdrop"onMouseDown={e=>{if(e.currentTarget===e.target)setOpen(null)}}><form className="modal"onSubmit={create}><div className="modal-head"><div><b>{open==='warehouse'?'إضافة مستودع':'إضافة صنف مخزني'}</b><span>سيتم إنشاء السجل داخل الجمعية الحالية فقط.</span></div><button type="button"onClick={()=>setOpen(null)}>×</button></div><label>الاسم بالعربية<input required value={form.nameAr}onChange={e=>setForm({...form,nameAr:e.target.value})}/></label><label>الاسم بالإنجليزية<input value={form.nameEn}onChange={e=>setForm({...form,nameEn:e.target.value})}/></label>{open==='item'&&<><label>SKU<input value={form.sku}onChange={e=>setForm({...form,sku:e.target.value})}/></label><label>الوحدة<input value={form.unit}onChange={e=>setForm({...form,unit:e.target.value})}/></label></>}<button className="primary wide"disabled={busy}>{busy?'جاري الحفظ...':'حفظ'}</button></form></div>}</main>}